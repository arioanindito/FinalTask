@model IEnumerable<DSS_MVC.Models.Status>

@{
    ViewData["Title"] = "Index";
}

<h1>Status Information</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StatusName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StatusName)
            </td>
            <td style="text-align: right">
                <a asp-action="Edit" asp-route-id="@item.StatusId">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.StatusId">Delete</a>
                <br />
                <div style="font-style:italic; font-size:10px">
                    Posted by @item.CreatedBy on @item.CreatedDate
                    <br />
                    Modified by @item.ModifiedBy on @item.ModifiedDate
                </div>
            </td>
        </tr>
}
    </tbody>
</table>
